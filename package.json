{
  "name": "dyslexia-reading-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that makes web articles easier to read for people with dyslexia",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.36",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
